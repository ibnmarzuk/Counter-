body {
  font-family: Arial, sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
}

.counter {
  display: flex;
  align-items: center;
}

button {
  font-size: 1rem;
  padding: 0.5rem;
  cursor: pointer;
}

#count {
  font-size: 1.5rem;
  margin: 0 1rem;
}
